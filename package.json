{
  "name": "shopfloor-workflow-optimization",
  "version": "1.0.0",
  "description": "Shopfloor Workflow Optimization System for MSME Manufacturing",
  "scripts": {
    "predev": "node scripts/kill-ports.js",
    "dev": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\" \"npm run server\" \"npm run client\"",
    "server": "node backend/server.js",
    "client": "npm run dev --prefix frontend",
    "seed": "node backend/seed.js",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
